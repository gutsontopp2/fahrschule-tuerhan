<section class="section">
  <div class="container page-narrow">
    <div class="section-head">
      <h1 style="font-size:clamp(1.7rem,5vw,2.4rem);">{{ page.title }}</h1>
      <hr class="roadline">
    </div>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{
  "name": "Seite",
  "settings": []
}
{% endschema %}
