{%- comment -%}
  Buchungsseite (Vorlage "buchung").
  Rendert den Buchungs-Kalender aus dem Snippet snippets/booking.liquid –
  bewusst kein Section-Schema, damit Shopify die Seite zuverlässig darstellt.
  Fahrstunden-Produkte kommen aus der Kollektion mit Handle "fahrstunden".
{%- endcomment -%}
{% render 'booking' %}
{% section 'cta-banner' %}
